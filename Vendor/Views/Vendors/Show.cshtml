@using VendorTracker.Models;

  <h1>Orders fot this vendor:</h1>

  @foreach (Order order in @Model["order"])
  {
    <li><a href='/vendor/@Model["vendor"].Id/orders/@order.Id'>@order.Name</a></li> <!-- RESOLVED opens order details -->
  }

  <p><a href='/vendor/@Model["vendor"].Id/orders/new'>Add New order</a> </p>  <!--RESOLVED -->
  <p><a href='/vendor'>Vendor's List</a></p>
  <p><a href='/'>Home</a></p>